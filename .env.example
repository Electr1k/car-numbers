# Переменные для docker-compose.prod.yaml; секреты сервисов — в их собственных .env
DOMAIN=nomerograf.ru

# База: 127.0.0.1 — только с сервера (SSH-туннель), 0.0.0.0 — открыть наружу
DB_BIND=127.0.0.1
DB_FORWARD_PORT=8989
